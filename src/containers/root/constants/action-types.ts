enum ActionTypes {
    ROOT_DEFINE_APPLICATION_STATE = 'Root: Define Application State',
    ROOT_DEFINE_LOCALE = 'Root: Define Locale',
    ROOT_INVOKE_API = 'Root: Invoke API',
}

export default ActionTypes;
